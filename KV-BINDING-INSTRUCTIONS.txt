# wrangler.toml — Cloudflare Pages config
#
# This file configures the KV namespace binding that Pages Functions need.
# Cloudflare Pages reads wrangler.toml at deploy time when functions/* exists.
#
# IMPORTANT: this is a SNIPPET. If you already have a wrangler.toml in the
# repo (for other Pages bindings, env vars, etc.), merge the [[kv_namespaces]]
# block below into your existing file rather than replacing it.

name = "ratehero"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# ---- KV namespace binding ----
# Bind the production KV namespace to the env variable PRICING_KV.
# Pages Functions access it via env.PRICING_KV.
#
# Steps to create the namespaces (run once, locally, with wrangler installed):
#
#   npx wrangler kv namespace create PRICING_KV
#   npx wrangler kv namespace create PRICING_KV --preview
#
# Wrangler prints the IDs. Paste them below, replacing the placeholders.

[[kv_namespaces]]
binding = "PRICING_KV"
id = "REPLACE_WITH_PRODUCTION_KV_NAMESPACE_ID"
preview_id = "REPLACE_WITH_PREVIEW_KV_NAMESPACE_ID"
